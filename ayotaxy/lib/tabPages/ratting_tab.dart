import 'package:flutter/material.dart';

class RattingTab extends StatelessWidget {
  const RattingTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Ratings"),
    );
  }
}
